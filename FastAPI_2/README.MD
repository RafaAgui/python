### Crear un entorno nuevo
python -m venv venv

### Activar el entorno
venv\Scripts\activate.bat

### Instalamos FASTAPI 
pip install fastapi[standard]

### Ejecutamos el servidor
fastapi dev main.py
